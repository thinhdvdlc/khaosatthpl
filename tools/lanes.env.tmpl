# Auto-generated bởi new-project. Sourced bởi bin/_common.sh.
HARNESS_ROOT="${HARNESS_ROOT:-__HARNESS__}"
LANES_ROOT="${LANES_ROOT:-__LANES_ROOT__}"
SOURCE_REPO="${SOURCE_REPO:-__SOURCE__}"
ORIGIN_URL="${ORIGIN_URL:-__GITLAB__}"
PROFILE="${PROFILE:-__SLUG__}"

PG_HOST="${PG_HOST:-127.0.0.1}"
PG_PORT="${PG_PORT:-5433}"
PG_USER="${PG_USER:-postgres}"
PG_PASS="${PG_PASS:-postgres}"
REDIS_HOST="${REDIS_HOST:-127.0.0.1}"
REDIS_PORT="${REDIS_PORT:-6379}"

DB_PREFIX="${DB_PREFIX:-__SLUG___l}"
API_BASE_PORT="${API_BASE_PORT:-8000}"
FE_BASE_PORT="${FE_BASE_PORT:-3000}"
WORKER_QUEUES="${WORKER_QUEUES:-default}"
COMPOSE_FILE="${COMPOSE_FILE:-$SOURCE_REPO/docker-compose.yml}"

if [ -f "$HARNESS_ROOT/config/secrets.env" ]; then
  # shellcheck disable=SC1091
  source "$HARNESS_ROOT/config/secrets.env"
fi
SEED_USER_PASSWORD="${SEED_USER_PASSWORD:-changeme}"
SEED_USER_EMAIL="${SEED_USER_EMAIL:-admin@__SLUG__.local}"
