#!/usr/bin/env bash
# seed — create the initial user/org. DATABASE_URL + SEED_USER_EMAIL/PASSWORD exported.
set -euo pipefail
# Clinical example:
#   ( cd "$LANE_DIR/$BACKEND_DIR" && uv run bash scripts/init_user.sh )
echo "harness: TODO — implement 'seed' for your stack (see profiles/clinical/hooks/seed.sh)" >&2
exit 1
